// Since JavaScript doesn't have TypeScript's interface and type definitions,
// we'll define empty objects for compatibility with the TypeScript version

// Message type
const Message = {};
 
module.exports = {
  Message
}; 